export function renderUserProfileScreen({ state, profile }) {
  return `
    <section class="screen ${state.route === 'userProfile' ? 'active' : ''}" data-screen="userProfile">
      <div class="card glass-card inner-hero-card"><div class="screen-hero-art user-hero-art"></div><div class="section-overline">User ID</div><h2 class="section-title premium-screen-title">Профиль пользователя</h2><p class="section-sub premium-screen-sub">Основные данные, детский профиль и состояние синхронизации в едином модуле.</p></div>
      <div class="card glass-card stat-strip dual-strip"><div class="premium-stat"><span class="premium-stat-label">Имя</span><b>${profile.name || 'Не заполнено'}</b></div><div class="premium-stat"><span class="premium-stat-label">Sync</span><b>${profile.serverSynced ? 'Cloud Linked' : 'Local Only'}</b></div></div>
      <div class="card answer-card action-list-card"><div class="list premium-action-list"><div class="item premium-action-item"><div class="item-title">Имя</div><div class="muted small">${profile.name || 'Пока не заполнено'}</div></div><div class="item premium-action-item"><div class="item-title">Email</div><div class="muted small">${profile.email || 'Пока не заполнено'}</div></div><div class="item premium-action-item"><div class="item-title">Имя ребёнка</div><div class="muted small">${profile.childName || 'Пока не заполнено'}</div></div><div class="status-panel"><div class="state-icon state-sync" aria-hidden="true"></div><div><div class="status-panel-title">Синхронизация</div><div class="muted small">${profile.serverSynced ? 'Есть серверные данные' : 'Пока только локальные данные'}</div></div></div></div><div class="row" style="margin-top:12px;"><button class="primary primary-wide" data-stub-action="sync-profile">Синхронизировать профиль</button><button class="secondary" data-nav="profileEditor">Редактировать</button><button class="secondary" data-nav="profile">Назад</button></div></div>
    </section>`;
}
